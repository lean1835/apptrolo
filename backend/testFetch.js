const http = require('http');
const fs = require('fs');
const token = fs.readFileSync('C:/Users/lean1/AppData/Local/Temp/token.txt', 'utf8').trim(); // Wait, where is the token stored? I didn't save it recently.
